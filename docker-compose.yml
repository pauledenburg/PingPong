web:
  build: .
  ports:
    - "80:80"
  links:
    - db
  volumes:
    - .:/var/www/html
db:
  image: "mysql:5"
  volumes:
    - ./pingpong.sql:/tmp/pingpong.sql
  environment:
    MYSQL_ROOT_PASSWORD: "root"
    MYSQL_USER: "pingponguser"
    MYSQL_PASSWORD: "VeryS3cure!"
    MYSQL_DATABASE: "pingpong"
